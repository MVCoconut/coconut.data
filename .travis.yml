sudo: required
dist: trusty

language: generic

os:
  - linux
      
install:
  - sudo npm install haxeshim -g
  - sudo npm install switchx -g
  - sudo npm install lix -g
  - switchx install
  - lix download
  - haxelib install travix

script:
  - haxelib run travix js
  - haxelib run travix node
